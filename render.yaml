services:
  - type: web
    name: pdf-highlighter   # lowercase, hyphen ok
    runtime: python
    branch: main
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app --bind 0.0.0.0:$PORT"
    plan: free
    envVars:
      - key: APP_DOMAIN
        sync: false
